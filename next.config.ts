import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ['images.pexels.com','lh3.googleusercontent.com'], // Allow images from Pexels
  },
};

export default nextConfig;
